/**
public class TreeNode {
    int val = 0;
    TreeNode left = null;
    TreeNode right = null;

    public TreeNode(int val) {
        this.val = val;

    }
}
*/
import java.util.*;
/*
 * similar to preOrder
 * visit node -> judge the sum ?= target
 */
public class Solution {
    public ArrayList<ArrayList<Integer>> findPath(TreeNode root,int target) {
    	ArrayList<ArrayList<Integer>> allPaths = new ArrayList<>();
    	ArrayList<Integer> onePath = new ArrayList<>();
    	int currentSum = 0;
    	findPathRecu(root, target, allPaths, onePath);
    	return allPaths;
    }
    private void findPathRecu(TreeNode root, int target, 
    		ArrayList<ArrayList<Integer>> allPaths, ArrayList<Integer> onePath){
    	if((root.left==null) && (root.right==null) && (root.val == target)){
    		onePath.add(root.val);
    		allPaths.add(onePath);
    		onePath.remove(onePath.size()-1);
    		return;
    	}
    	if(root.val > target) {
    		onePath.remove(onePath.size()-1);
    		return;
    	}
    	if(root.left != null){
    		onePath.add(root.val);
    		findPathRecu(root.left, target-root.val, allPaths, onePath);
    	}
    	if(root.right != null){
    		onePath.add(root.val);
    		findPathRecu(root.right, target-root.val, allPaths, onePath);
    	}
    		
    	
    }
}
